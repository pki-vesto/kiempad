# Kiempad — Codex Fix Plan (Code, UI & UX Review)

> Review-only backlog for Codex. Generated by the Kiempad review agent. **Do not treat any
> section as done** until the acceptance criteria are met. References are to real files/lines at
> review time (`main` @ `f33e9ae`); line numbers drift, so grep the named symbols.
>
> Source of truth for visual target: `Kiempad.dc.html` (Claude Design), `design_handoff_kiempad_redesign/README.md`,
> `docs/VERBETERINGEN-VOLLEDIG.md` (108-point audit), `docs/UI_GUIDELINES.md`, `docs/CLAUDE_DESIGN_PROMPT.md`.

---

## Implementation Progress

- **CFX-030 — done in G1320 / issue #2415.** `index.html` now defines Claude Design teal
  `theme-color` for light mode, a dark-mode variant, and no longer contains the old sage
  `#7a9471`; `manifest.webmanifest` now uses teal `theme_color` and the Kiempad page background.
- **CFX-010/CFX-081 — done in G1321 / issue #2417.** Mobile/tablet navigation now shows 8
  primary tabs plus a `Meer` sheet instead of all 13 routes at once. Secondary routes stay
  reachable in grouped Inzicht/Beheer/Privacy sections, and hidden active routes keep the
  `Meer` trigger active.

---

## Executive Summary

Kiempad is a local-first / central-encrypted IVF-ICSI companion PWA built **without a framework**:
UI is rendered as HTML-template strings and re-bound after every `innerHTML` replace. The domain
layer is mature (42 modules under `src/domain/`, 86 test files, a real central-encrypted backend in
`src/server/` + `src/storage/`). The **visual identity has been migrated to the Claude Design teal
system** (tokens, self-hosted Instrument Serif + Hanken Grotesk, hairline cards) and the FIXES.md
round (buttons, nav, forms, empty-state CSS, unlock screen) is largely landed.

The product is **not yet premium**. Three structural problems dominate:

1. **`src/appShell.ts` is an 18,540-line monolith** with **354 `render*` functions**; the single
   worst is `renderDossierScreen` at **1,045 lines**. All screens, all forms, all copy, and all
   markup live in one file. This makes every change high-risk and every review shallow.
2. **The component layer is half-adopted.** `src/ui/components.ts` defines `emptyState`,
   `loadingSkeleton`, `card`, `actionCard`, `timeline`, `accordion`, `statRow`, etc., but
   `emptyState()` has **0 call sites** and `loadingSkeleton` is **unused** — screens still emit 47
   raw `<p class="empty-state">` strings and there are **no loading/skeleton states anywhere**.
   The abstractions exist but the screens don't use them.
3. **`styles.css` is 14,343 lines** and carries a compatibility **alias layer** (old sage token
   names aliased to new teal tokens). This works but hides the real system and invites drift
   (CFX-030 has since moved PWA chrome from old sage to Claude Design teal; remaining token alias
   cleanup is tracked separately).

UX-wise it still reads as **a dense, technical tool in places**: developer jargon in user copy
("command center", "Imaging-repository", "Dossierindex", "Hersteldiagnose"-style diagnostics),
"Eigenaar: man/vrouw" instead of names, "Dagelijkse aanbevelingen" (which collides with the
"no medical advice" stance), a greeting hardcoded to "Peter & partner" (no names setting), no real
mobile "Meer" sheet / Settings sheet, and no seed/example data so empty installs feel hollow.

This plan gives Codex a prioritized path: **de-risk the architecture first** (split the monolith,
adopt the component layer), then **finish the design system + screens + copy**, then **fill
mobile/a11y/loading/seed gaps**, then **harden tests**.

---

## Highest Priority Fixes (Top 20)

| # | Fix | Why |
|---|-----|-----|
| 1 | Split `appShell.ts` (18.5k LOC) into per-screen modules under `src/screens/` | Every change is currently high-risk; blocks all other work |
| 2 | Break up `renderDossierScreen` (1,045 LOC) into upload/imaging/embryo/history sub-renderers | Largest single fragility hotspot |
| 3 | Adopt `emptyState()` everywhere (47 raw `<p class="empty-state">`, 0 component calls) | Empty installs feel hollow; component already exists |
| 4 | Add `loadingSkeleton()` states for async lists (dossier, timeline, AI, OCR) — currently none | App "pops" abruptly; feels like a prototype |
| 5 | ✅ CFX-030: Fix `theme-color` in `index.html` (`#7a9471` sage → teal `#2c6e63`) | Done in G1320; PWA/browser chrome now uses teal |
| 6 | Build the Settings sheet (`eigenNaam`/`partnerNaam`/`gedeeldeModus` + thema, no "Bewaar thema") | VERBETERINGEN D32; unblocks greeting/names everywhere |
| 7 | Wire real names into the greeting (`renderStartCommandHeader` hardcodes "Peter & partner") | Core personalization; currently a placeholder |
| 8 | De-jargon user copy app-wide ("command center", "repository", "Dossierindex", man/vrouw) | VERBETERINGEN H54–56; reads as a dev tool |
| 9 | Rename "Aanbeveling/Dagelijkse aanbevelingen" → "Suggestie"/"Te doen vandaag" | Collides with "no medical advice"; medical-safety copy |
| 10 | Implement the mobile "Meer" nav sheet + Beheer/Privacy grouping | VERBETERINGEN D34/D37, L81/L85; nav is desktop-shaped |
| 11 | Seed/example-data mode ("Voorbeelddata laden") for empty datasets | Reviewers/first-run see a hollow app (VERBETERINGEN E39) |
| 12 | Adopt the form-field kit on **every** form (not just `.data-form`) | Native date/number/select fields still leak in places |
| 13 | Remove the big rounded grey frame around the content column (VERBETERINGEN I61) | Content should rest on the page bg; depth from cards |
| 14 | Dark-mode sweep (contrast of `--ink-2/-3` on `--surface`, card borders) | VERBETERINGEN G50; unverified after token migration |
| 15 | `env(safe-area-inset-bottom)` + non-overlap on the mobile bottom-nav | VERBETERINGEN L83; unusable on notched phones |
| 16 | Consistent status-badge component (Vergoed/Eigen risico/AI/Arts-geverifieerd) | VERBETERINGEN J68; badges are ad-hoc per screen |
| 17 | Single toggle-switch component (Herinneringen, Gedeelde modus) | VERBETERINGEN J71; currently bespoke |
| 18 | Success feedback after save (toast/inline) — most forms save silently | VERBETERINGEN C31/M; users don't know it worked |
| 19 | Replace brittle substring assertions with a screen-render snapshot + interaction tests | `appShell.test.ts` is a giant substring wall; refactor-hostile |
| 20 | Extract a `renderScreen(id)` dispatch + shared screen chrome (header/policy panel) | Removes copy-paste chrome across 13 screens |

---

## UI Architecture Problems

- **God-module**: `src/appShell.ts` (18,540 LOC, 354 `render*`). No module boundaries; screens,
  components, copy, and the vault gate all coexist. Impossible to review or test in isolation.
- **Rendering model**: `render()` in `src/main.ts` sets `innerHTML` then re-binds listeners via
  `querySelector('#id' | '.class' | '[data-*]' | '[name=]')`. Restructuring markup silently breaks
  bindings unless every id/name/data/aria/class is preserved. There is **no event delegation** and
  **no diffing** — full teardown/rebuild on every state change loses focus/scroll and forbids
  animation. This is the deepest fragility in the app.
- **Half-adopted component layer**: `src/ui/components.ts` is good but under-used. `emptyState()` = 0
  calls, `loadingSkeleton` = 0 calls, `timeline()`/`accordion()`/`statRow()` used inconsistently;
  screens still hand-roll equivalent markup. Two sources of truth per pattern.
- **CSS size & aliasing**: `styles.css` = 14,343 lines with an old→new token alias layer. Works, but
  the alias layer means "the real design system" isn't legible; dead/duplicated rules likely. No CSS
  layering (`@layer`), no obvious componentized structure.
- **Copy embedded in markup**: user-facing strings (incl. medical disclaimers) are inline in
  `appShell.ts`, duplicated across cards (VERBETERINGEN N95 — disclaimer repeated per card). No
  copy module → inconsistent wording and jargon leaks.
- **Screen chrome duplication**: each `render*Screen` re-emits page-header + policy/disclaimer
  panel. No shared `screenShell()` wrapper.
- **Sub-routes bolted on**: `SCREENS` mixes primary routes (`start`…`backup`) with sub-routes
  (`overzicht`, `recent`, `categorieen`, `privacy`, `prepare`, `compare`, `choice`) — IA is unclear.

---

## UX Problems

- **Hollow empty state**: fresh install shows 47 flat "Nog geen … vastgelegd" lines and no data →
  feels like an unfinished prototype. No seed data, no rich empty blocks in use.
- **No loading feedback**: async work (OCR, AI summaries, central fetch) appears with no skeleton or
  spinner; the UI just pops.
- **Developer/clinical language**: "command center", "Imaging-repository", "Dossierindex",
  "Consult Prep Wizard", "Hersteldiagnose", "Eigenaar: man/vrouw", "client-side", "repository".
  Non-technical users (a couple in IVF) will feel talked-down-to or lost.
- **"Aanbeveling" framing** conflicts with the app's own "geen medisch advies" stance — emotionally
  and legally the wrong word.
- **Personalization is fake**: greeting says "Hoi Peter & partner" literally; no names setting.
- **Silent saves**: forms submit with no confirmation; users can't tell if it worked.
- **Mobile is desktop-shaped**: the sidebar/nav was built desktop-first; no real "Meer" sheet, no
  bottom-sheets for Settings, no safe-area handling.
- **Density**: some screens (Dossier, Kennis, Vragen) still present walls of controls rather than a
  focused, card-and-hero flow.
- **Disclaimer fatigue**: the medical disclaimer repeats on many cards instead of once per screen.

---

## Screen-by-Screen Review

> Format: current → problem → desired → required fixes. Cross-refs to `docs/VERBETERINGEN-VOLLEDIG.md`.

### Vandaag / Start (`renderStartScreen`, `renderStartCommandHeader`, `renderDailyCommandCenter`, `renderStartNextStepBoard`)
- **Current**: greeting header (hardcoded "Peter & partner"), fase-hero, command-center triage,
  next-step board, "Vandaag te zetten" dose list, daily suggestions card, quick-entry disclosure.
- **Problem**: hardcoded names; "command center" jargon; suggestions still labelled "Aanbeveling";
  no seed data so it's mostly empty; command-center "Nu eerst/Later/Context" are plain text not
  cards with eyebrows (VERBETERINGEN O96).
- **Desired**: warm personalized greeting from Settings; calm triage cards with eyebrows; suggestion
  cards (renamed); populated demo state on first run.
- **Fixes**: CFX-006/007/009/011/033.

### Traject (`renderTrajectScreen`, `fertilityTimeline.ts`)
- **Current**: eyebrow + serif title, vergoeding info, fase list.
- **Problem**: timeline is not the dot/line component with amber "current" card (VERBETERINGEN
  O97/J73); filter fields unstyled by the kit.
- **Desired**: vertical dot/line timeline, current step as amber-left-border card; kit fields.
- **Fixes**: CFX-020, CFX-012.

### Agenda (`renderAgendaScreen`)
- **Current**: sections + appointment rows.
- **Problem**: no date-block + `border-left` colored card; "Verwacht" not dashed; "Toevoegen" not a
  pill (VERBETERINGEN O98).
- **Fixes**: CFX-034.

### Medicatie (`renderMedicatieScreen`, `renderDoseLogList`)
- **Current**: "Vandaag" dose list + middelen + add/import.
- **Problem**: no progress bar; dose cards lack the 3-status visual (gepland/genomen/overgeslagen)
  and injectie `--clay` border; buttons still plain (VERBETERINGEN O99).
- **Fixes**: CFX-035.

### Vragen (`renderVragenScreen`)
- **Current**: open/answered lists + input.
- **Problem**: "Consult Prep Wizard" jargon; Print/PDF button floats; input not a pill
  (VERBETERINGEN O100).
- **Fixes**: CFX-036, CFX-008.

### Kosten (`renderKostenScreen`)
- **Current**: stat cards + list + form.
- **Problem**: amounts not right-aligned `tabular-nums`; badges ad-hoc; form not via kit; bare
  empty state (VERBETERINGEN O101/J68/I66).
- **Fixes**: CFX-016, CFX-037.

### Kennis (`renderKennisScreen`, 253 LOC)
- **Current**: search + categories + accordion cards.
- **Problem**: "0 van 0 item(s)" shows when empty; search/category not kit-styled; jargon
  (VERBETERINGEN O102/E42).
- **Fixes**: CFX-038, CFX-042.

### Welzijn (`renderWelzijnScreen`)
- **Current**: owner segments + mood buttons + counts + check-ins.
- **Problem**: "Eigenaar: man/vrouw" instead of names/"Samen" (VERBETERINGEN O103/H55).
- **Fixes**: CFX-039, CFX-007.

### Dossier (`renderDossierScreen`, **1,045 LOC**) + upload/imaging/embryo/consult
- **Current**: routes upload/search/imaging/timeline; document rows, imaging thumbnails, embryo
  quality, consult verslagen, behandelgeschiedenis.
- **Problem**: single 1,045-line function; jargon ("Imaging-repository", "Dossierindex"); OCR/upload
  status is technical; thumbnails placeholder; bare empties (VERBETERINGEN O104).
- **Desired**: split into sub-renderers; de-jargoned NL; kit filters; encrypted thumbnail tile;
  rich empty states.
- **Fixes**: CFX-002, CFX-008, CFX-040.

### Afwegingen (`renderAfwegingenScreen`, `decision.ts`)
- **Problem**: decision cards lack option +/− tags + chosen banner; form not via kit; bare empty
  (VERBETERINGEN O105).
- **Fixes**: CFX-041.

### Herinneringen (`renderHerinneringenScreen`)
- **Problem**: rows need time+title+source+toggle-switch component; Vandaag/Deze week sections
  (VERBETERINGEN O106/J71).
- **Fixes**: CFX-017.

### Logboek (`renderLogboekScreen`, `eventLog.ts`)
- **Problem**: "Gebeurtenislog" heading can still break mid-word on narrow; timeline rows; framing as
  "what happened to your data" (VERBETERINGEN O107/N93).
- **Fixes**: CFX-020, CFX-043.

### Back-up (`renderBackupScreen`, 199 LOC) + WebAuthn enroll
- **Problem**: jargon; export card should be preview + Download/Kopieer (partly done for timeline
  export only); "Laatste back-up" reminder; still has a `Biometrie/WebAuthn` heading
  (VERBETERINGEN O108).
- **Fixes**: CFX-044, CFX-008.

### Ontgrendelscherm (vault gate `renderVaultGate`) — **recently redesigned (point 109), landed**
- **Residual**: `renderStorageBootstrapError` still uses "Centrale encrypted opslag" jargon and a
  technical checklist; diagnostics `<details>` still contains dev labels. Solo/shared "je/jullie"
  can't bend until names exist.
- **Fixes**: CFX-045.

---

## Component System Gaps

- **`emptyState()` unused (0 calls)** → adopt on all 47 sites with per-screen icon + title + CTA.
- **`loadingSkeleton()` unused** → no loading states; add for async lists.
- **No Settings sheet / modal-sheet primitive** — needed for D32/D34/E and future flows.
- **No toggle-switch component** — Herinneringen + Gedeelde modus reinvent it.
- **No status-badge component** — badges are inline per screen with drifting colors.
- **No `screenShell()` / page chrome wrapper** — header + policy panel copy-pasted per screen.
- **No amount/number cell** (`tabular-nums`, right-aligned, `€` adornment) — Kosten needs it.
- **No confirm/toast primitive** — destructive actions use `window.confirm`; saves are silent.
- **No date-input adornment** (calendar icon, real placeholder) — native date leaks.
- **Copy module missing** — disclaimers/labels should be centralized constants, not inline.

---

## Data & Backend Gaps

- **Dual storage path complexity**: `src/storage/clientStorage.ts` + `centralHttpApi.ts` +
  `centralHealthContract.ts` support `central-api` and `legacy-indexeddb`. `main.ts` branches on
  `state.storageMode` in many places (≈10+ inline checks) — extract a storage-mode strategy so
  screens don't re-derive labels/copy.
- **Names/gedeelde modus not persisted**: `types.ts` mentions `gedeeldeModus` but `AppSettings`
  (`src/domain/settings.ts`) has no `eigenNaam`/`partnerNaam`/`gedeeldeModus`; the Settings sheet
  and greeting depend on adding them (local, encrypted).
- **Upload validation**: `main.ts` dossier-upload path (`readAsDataURL`, `lokaleOcr`) — verify file
  type/size limits, reject unsupported types with an inline message, and cap memory (large images).
- **OCR is technical**: OCR status strings surface to users; make them calm and optional.
- **No optimistic UI / retry** on central fetch failures beyond the error alert; consider a queued
  retry with clear state.
- **Seed data**: no example dataset generator for demo/review (E39).
- **Backup reminder** (`backupReminder.ts`) surfaces as small print; make it a first-class card.

---

## Security & Privacy Gaps

- **Disclaimer repetition** (VERBETERINGEN N95): the "geen medisch advies" line repeats on many
  cards → centralize to one per screen (footer). Repetition also dilutes trust.
- **Privacy wording inconsistent** (N91): "Centrale encrypted opslag" vs "lokaal" vs "versleuteld
  gesynct" — pick one clear phrase and use it everywhere (vault gate now says
  "Versleuteld · alleen op dit toestel"; bootstrap-error + Dossier still differ).
- **Encrypted thumbnails**: imaging thumbnail tile is a placeholder; confirm the real path renders
  the decrypted-in-memory thumbnail only and never persists plaintext (there is a
  `dossierPrivacy.test.ts` — extend it to cover the render path).
- **Error redaction is good** (vault gate sanitizes raw errors + support-handoff); keep that
  contract when refactoring — it is covered by `appShell.test.ts` unlock-error tests. Do not weaken.
- **`window.confirm` for deletes** is jarring and unstyled; replace with an in-app confirm that
  matches the calm tone but still blocks accidental medical-data loss.
- **CSP / inline SVG**: current approach uses inline SVG + `data:` chevrons (allowed). Keep the
  asset-scan green (`npm run assets:check`) — no remote URLs.

---

## Accessibility Gaps

- **Good baseline**: `index.html` has `lang="nl"`; 433 `aria-` attributes in `appShell.ts`.
- **Focus management lost on re-render**: full `innerHTML` replace drops focus/scroll (e.g. after
  saving, keyboard users lose their place). Needs focus restoration or targeted DOM updates.
- **No visible loading semantics**: async regions lack `aria-busy` / live regions.
- **Toggle/checkbox semantics**: bespoke toggles must expose `role="switch"` + `aria-checked`.
- **Hit targets**: verify ≥44px on mobile nav tabs, toggles, the vault eye-toggle, `⋯` menu.
- **Contrast**: re-check `--ink-3` on `--surface` for body text (meta only) after token migration;
  verify dark mode.
- **Details/summary menus** (`⋯`, vault details) need keyboard + `aria-expanded` verification.
- **Icon-only buttons** (eye toggle, `⋯`, delete) — confirm every one has an `aria-label`.

---

## Testing Gaps

- **`appShell.test.ts` is a massive substring wall** — brittle, refactor-hostile (every copy tweak
  breaks N assertions, as seen in the FIXES rounds). Move to: (a) per-screen render smoke that
  asserts structural anchors (ids/roles) not prose; (b) a small set of copy-contract tests for the
  security-sensitive strings only.
- **No interaction/DOM tests**: bindings in `main.ts` (dose toggle, eye toggle, filters, uploads)
  are untested at the DOM level. Add jsdom/Playwright interaction tests for the critical flows
  (unlock, dose afvinken, dossier upload, add question).
- **No visual regression**: Playwright is a devDep but there's no screenshot baseline for the
  redesign; add smoke screenshots for the 13 screens + vault gate at 360/768/1280px.
- **Upload/OCR flow undertested**: assert file-type/size rejection, thumbnail render, no plaintext
  persistence.
- **Loading/empty/error states untested** because they don't exist yet — add once implemented.
- **Component tests** (`tests/ui/components.test.ts`) exist but don't cover `emptyState` rich
  variants (icon/title) or `loadingSkeleton` variants — extend as adoption lands.

---

## Codex Implementation Backlog (≥50 items)

> Priority: P0 (blocker) · P1 (high) · P2 (medium) · P3 (polish). Complexity: S/M/L/XL.

### Architecture & rendering
- **CFX-001** — Split `appShell.ts` into `src/screens/<screen>.ts`. Problem: 18.5k-LOC monolith.
  Outcome: one module per screen exporting a `render*Screen`. Files: `src/appShell.ts` → `src/screens/*`.
  Accept: appShell becomes a thin dispatcher; all tests pass; no file >1500 LOC. **P0 · XL**
- **CFX-002** — Decompose `renderDossierScreen` (1,045 LOC) into `renderDossierUpload`,
  `renderDossierImaging`, `renderDossierEmbryo`, `renderDossierHistory`. Outcome: each <200 LOC.
  Files: `src/appShell.ts`. Accept: identical DOM output (snapshot), all bindings preserved. **P0 · L**
- **CFX-020b** — Introduce `screenShell({title,eyebrow,intro,children,disclaimerScope})` wrapper to
  remove per-screen header + policy-panel duplication. Files: `src/ui/components.ts`, all screens.
  Accept: disclaimer rendered once per screen. **P1 · L**
- **CFX-003** — Add event delegation + focus/scroll restoration in `render()` so re-renders don't
  drop keyboard focus. Files: `src/main.ts`. Accept: after a save, focus returns to a sensible
  element; scroll position kept (VERBETERINGEN M90). **P1 · L**
- **CFX-004** — Extract a `storageMode` strategy object (labels, copy, capabilities) so screens/
  `main.ts` stop branching on `'central-api'`. Files: `src/storage/*`, `src/main.ts`, `src/appShell.ts`.
  Accept: <3 inline `storageMode ===` checks remain. **P2 · M**
- **CFX-005** — Central copy module `src/copy.ts` for disclaimers/labels; screens import constants.
  Accept: the medical disclaimer string is defined once. **P1 · M**

### Design system & tokens
- **CFX-030** — ✅ Done in G1320 / issue #2415. Fix `index.html` `theme-color`
  `#7a9471` → `#2c6e63` (+ dark variant via media). Files: `index.html`,
  `public/manifest.webmanifest`. Accept: PWA chrome is teal. **P0 · S**
- **CFX-031** — Collapse the old→new token alias layer in `styles.css`: migrate component rules to
  canonical `--ink/--line/--teal…` names, then delete unused aliases. Files: `src/styles.css`.
  Accept: no `--accent/--text/--border` aliases remain referenced; visual diff unchanged. **P2 · XL**
- **CFX-032** — Introduce `@layer tokens, base, components, screens, utilities` and reorganize
  `styles.css` (14.3k LOC) accordingly; split into imported partials if the build allows.
  Accept: no specificity regressions; file(s) navigable. **P2 · L**
- **CFX-013** — Remove the large rounded grey frame around the content column (I61); content rests
  on page bg. Files: `src/styles.css`. Accept: only cards carry borders. **P1 · S**
- **CFX-014** — Dark-mode contrast sweep (G50): audit `--ink-2/-3` on `--surface`, card borders,
  no pure-white blocks. Accept: WCAG AA for body text in dark. **P1 · M**
- **CFX-062** — Vertical rhythm tokens (I62): screen padding desktop 38×52 / mobile 16×22, section
  gap ~24, card gap 10–11; content max-width 720 centered. Accept: consistent across screens. **P2 · M**

### Components
- **CFX-003c** — Adopt `emptyState()` on all 47 `<p class="empty-state">` sites with per-screen
  icon + serif title + one CTA (E38/E40). Files: `src/appShell.ts`, `src/ui/components.ts`.
  Accept: 0 raw `empty-state` `<p>`; each screen's primary empty uses the rich block. **P1 · L**
- **CFX-004c** — Add `loadingSkeleton()` states for dossier list, timeline, AI summaries, OCR, and
  central fetch. Files: screens + `src/main.ts`. Accept: async regions show skeleton + `aria-busy`. **P1 · M**
- **CFX-006** — Settings sheet component (`renderSettingsSheet`) + open/close state (`settingsOpen`)
  as a centered modal (desktop) / bottom-sheet (mobile); contains Namen, Gedeelde modus, Thema
  (applies immediately, no "Bewaar thema"). Files: `src/appShell.ts`, `src/main.ts`, `src/styles.css`.
  Accept: names persist to encrypted Settings; theme toggles live. **P0 · L**
- **CFX-017** — Toggle-switch component (`toggle({name,checked,label})`) with `role="switch"`;
  adopt in Herinneringen + Gedeelde modus. Accept: single implementation, keyboard-operable. **P1 · M**
- **CFX-016** — Status-badge component (`badge({label,tone})`) with the J68 color map; adopt in
  Kosten/Kennis/Dossier. Accept: badges consistent 10.5px pill. **P1 · M**
- **CFX-018** — Toast/inline success feedback primitive; fire after every store save. Accept: saves
  show a calm confirmation (VERBETERINGEN C31/M). **P1 · M**
- **CFX-019b** — In-app styled confirm to replace `window.confirm` for deletes. Files:
  `src/deleteConfirmations.ts`, `src/main.ts`. Accept: no native `confirm()`; still blocks. **P2 · M**
- **CFX-066** — Amount cell (`tabular-nums`, right-aligned, `€` adornment) for Kosten. **P2 · S**

### Copy / language (H)
- **CFX-008** — De-jargon sweep: "command center"→"dagoverzicht", "Imaging-repository"→"Beelden",
  "Dossierindex"→"Overzicht", "Consult Prep Wizard"→"Consult voorbereiden", "repository/client-side"
  → plain NL. Files: `src/appShell.ts` (+ copy module CFX-005). Accept: no listed terms remain in
  user-visible strings. **P1 · L**
- **CFX-009** — Rename "Aanbeveling/Dagelijkse aanbevelingen" → "Suggestie"/"Te doen vandaag".
  Files: `src/appShell.ts`, `src/domain/dailyRecommendations.ts` (labels only, not logic).
  Accept: no user-visible "aanbeveling". **P1 · M**
- **CFX-007** — Replace "Eigenaar: man/vrouw" with names + "Samen" everywhere (greeting, Welzijn
  owner segments, check-in "Van wie"). Depends on CFX-006. Accept: no "man/vrouw" owner labels. **P1 · M**
- **CFX-059** — Shorten + standardize disclaimers to one sentence per context (N59/N95). **P2 · M**
- **CFX-057** — Spelling pass on repeated disclaimer/source strings (H57). **P3 · S**

### Navigation & mobile (D/L)
- **CFX-010** — ✅ Done in G1321 / issue #2417. Mobile "Meer" sheet: bottom-nav shows 8 primary + "Meer" (opens sheet with Beheer +
  Privacy groups + Instellingen). Files: `src/appShell.ts`, `src/styles.css`, `src/main.ts`.
  Accept: all 13 routes reachable on mobile; active state reflected. **P1 · L**
- **CFX-034b** — Nav grouping (D34): Primair(8)/Beheer(3)/Privacy(2) with eyebrow labels + hairline
  separators in the desktop sidebar. **P2 · M**
- **CFX-033b** — Move theme + lock controls out of the sidebar top into the Settings sheet / a quiet
  bottom-of-sidebar action (D32/D33). Depends on CFX-006. **P1 · M**
- **CFX-015** — `env(safe-area-inset-bottom)` + non-overlap + scrollable bottom-nav (L83). **P1 · S**
- **CFX-082** — Mobile: forms stack single-column; buttons full-width only where needed (L82). **P2 · S**
- **CFX-081** — Verify desktop = sidebar + 720px content; mobile = topbar + bottom-nav + sheets;
  fix any full-bleed screens (L81/I63). **P1 · M**

### Screens (from Screen-by-Screen)
- **CFX-020** — Timeline component adoption for Traject + Logboek (dot/line rail, amber current
  card). Files: `src/appShell.ts`, `src/domain/fertilityTimeline.ts` (view only). **P1 · L**
- **CFX-034** — Agenda: date-block + colored `border-left` card, dashed "Verwacht", "Toevoegen"
  pill. **P2 · M**
- **CFX-035** — Medicatie: progress bar + 3-status dose cards + injectie `--clay` border. **P1 · M**
- **CFX-036** — Vragen: rename wizard, pill input, dock Print/PDF next to the section head. **P2 · M**
- **CFX-037** — Kosten: stat cards + info + list with badges + right-aligned amounts + kit form +
  rich empty. **P1 · M**
- **CFX-038** — Kennis: kit search/category, category-eyebrow cards, chevron, badges. **P2 · M**
- **CFX-042** — Kennis: hide "0 van 0 item(s)"; show empty block instead (E42). **P2 · S**
- **CFX-039** — Welzijn: owner segments with names, mood buttons, count cards, check-in list. **P1 · M**
- **CFX-040** — Dossier: kit filters, encrypted thumbnail tile, de-jargon, rich empties (after
  CFX-002). **P1 · L**
- **CFX-041** — Afwegingen: decision cards with +/− tags + chosen banner + kit form + empty. **P2 · M**
- **CFX-043** — Logboek: read-only "what happened to your data" framing + timeline rows. **P2 · S**
- **CFX-044** — Back-up: export preview + Download/Kopieer, "Laatste back-up" reminder card,
  de-jargon. **P2 · M**
- **CFX-045** — `renderStorageBootstrapError` + Dossier: apply the unified privacy phrasing +
  de-jargon (N91). **P2 · S**

### Forms (C)
- **CFX-012** — Apply the form-field kit to every form (not just `.data-form`): Traject filter,
  Dossier filters, Kennis search, Research, Welzijn/Vastleggen, snelle invoer. Accept: no native
  date/number/select styling anywhere. **P1 · L**
- **CFX-025** — `€` prefix adornment in amount fields (C25). **P3 · S**
- **CFX-026** — Real date placeholder ("dd-mm-jjjj") vs grey filled-looking default (C26). **P2 · S**
- **CFX-027** — Textarea kit: `min-height:74px`, `resize:vertical`, kit border everywhere (C27). **P2 · S**
- **CFX-028** — Form actions: primary Bewaar + ghost Annuleer, not both full-width teal (C28). **P2 · S**
- **CFX-029** — Inline validation in `--clay-ink` (no browser popups) (C29). **P2 · M**
- **CFX-030f** — Forms in a card with title + `✕` close, padding 20, radius 18 (C30). **P2 · M**

### Data / backend / seed
- **CFX-011** — Seed/example-data mode: generator (3 doses, 2 afspraken, 3 check-ins, some kennis) +
  "Voorbeelddata laden" button, wipeable. Files: new `src/domain/seed.ts`, `src/main.ts`,
  Settings sheet. Accept: reviewer can toggle a populated demo state. **P1 · L**
- **CFX-046** — Add `eigenNaam`/`partnerNaam`/`gedeeldeModus` to `AppSettings` +
  `normaliseerAppSettings` + store setters. Files: `src/domain/settings.ts`,
  `src/domain/settingsStore.ts`. Accept: persisted, encrypted, defaulted. **P0 · M**
- **CFX-047** — Upload validation: file type/size allowlist + inline rejection + memory cap. Files:
  `src/main.ts` dossier-upload handler. Accept: unsupported/oversized files rejected calmly. **P1 · M**
- **CFX-048** — Central-fetch retry/queued state with clear UI (beyond the error alert). **P2 · L**
- **CFX-049** — Backup reminder as a first-class card (from `backupReminder.ts`). **P3 · S**

### Loading / empty / error states
- **CFX-050** — Standard error banner adoption (`errorBanner()`) for all failed async, not ad-hoc
  `.form-error`. **P2 · M**
- **CFX-051** — `aria-busy` + live-region announcements during async work. **P2 · S**
- **CFX-052** — Per-screen empty icons + copy map (E40) once CFX-003c lands. **P2 · S**

### Accessibility
- **CFX-053** — Audit every icon-only button for `aria-label` (eye, `⋯`, delete, toggles). **P1 · S**
- **CFX-054** — Hit-target ≥44px audit on mobile nav/toggles/menus. **P2 · S**
- **CFX-055** — `role="switch"`/`aria-checked` on all toggles (with CFX-017). **P1 · S**
- **CFX-056** — Contrast audit (`--ink-3` body text, dark mode) + fixes (K74). **P1 · M**
- **CFX-075** — Verify visible focus ring (teal) on all interactive elements incl. `<details>`
  summaries (K75). **P2 · S**
- **CFX-079** — Reduced-motion coverage for every new transition/skeleton. **P2 · S**

### Testing
- **CFX-070** — Refactor `appShell.test.ts` into per-screen render smoke (structural anchors) +
  a small copy-contract suite for security-sensitive strings only. **P1 · L**
- **CFX-071** — DOM interaction tests (jsdom/Playwright) for unlock, dose toggle, eye toggle,
  dossier upload, add question. **P1 · L**
- **CFX-072** — Playwright visual smoke: 13 screens + vault gate at 360/768/1280px, light+dark. **P2 · L**
- **CFX-073** — Upload/OCR tests: file-type/size rejection, thumbnail render, no plaintext
  persistence (extend `dossierPrivacy.test.ts`). **P1 · M**
- **CFX-074** — Component tests for `emptyState` rich variants + `loadingSkeleton` + `toggle` +
  `badge`. **P2 · S**

### Security / privacy
- **CFX-091** — Unify privacy phrasing app-wide (N91); one string in the copy module. **P1 · S**
- **CFX-092** — Slot/shield icon usage rationalized; not on every card (N92). **P3 · S**
- **CFX-095** — One medical disclaimer per screen (footer), remove per-card repetition (N95). **P1 · M**
- **CFX-094** — AI/Artscheck labels calm-but-clear that summaries are unverified until a clinician
  confirms (N94). **P2 · S**

---

## Recommended Execution Order

1. **De-risk (P0 architecture)**: CFX-030 (theme-color), CFX-046 (settings model), CFX-001 (split
   appShell), CFX-002 (split Dossier), CFX-005 (copy module). These unblock everything and are
   individually shippable.
2. **Adopt the component layer**: CFX-003c (emptyState), CFX-004c (loadingSkeleton), CFX-006
   (Settings sheet), CFX-017 (toggle), CFX-016 (badge), CFX-018 (toast).
3. **Personalize + de-jargon**: CFX-007 (names), CFX-008 (jargon), CFX-009 (Aanbeveling→Suggestie),
   CFX-091/095 (privacy/disclaimer copy).
4. **Mobile + nav**: CFX-010 (Meer sheet), CFX-033b (theme/lock relocation), CFX-015 (safe-area),
   CFX-081/082 (responsive).
5. **Screens** (one PR each): CFX-020 (timeline) → CFX-035 (Medicatie) → CFX-037 (Kosten) →
   CFX-039 (Welzijn) → CFX-040 (Dossier) → CFX-038/042 (Kennis) → CFX-034 (Agenda) →
   CFX-036 (Vragen) → CFX-041 (Afwegingen) → CFX-043 (Logboek) → CFX-044 (Back-up).
6. **Forms polish**: CFX-012 then CFX-025–030f.
7. **Loading/empty/error + a11y**: CFX-050/051/052, CFX-053–079.
8. **Seed + backend hardening**: CFX-011, CFX-047, CFX-048, CFX-004 (storage strategy), CFX-049.
9. **Design-system cleanup**: CFX-031 (de-alias), CFX-032 (@layer), CFX-013/014/062.
10. **Testing hardening throughout**: CFX-070–074 land alongside the areas they cover.

> Guardrails for Codex: keep every PR CI-green (`typecheck`, `lint`, `test`, `build`,
> `assets:check`), preserve all bound `id`/`name`/`data-*`/`aria-*`/asserted classes when
> restructuring markup, and never weaken the vault-gate error sanitization / support-handoff
> contract. Network: none. Data: local/encrypted only. Medical: disclaimers preserved.
